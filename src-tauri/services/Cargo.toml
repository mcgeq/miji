[package]
name = "services"
version = "0.1.0"
edition = "2024"
description = "Aggregated business logic coordinating workflows across multiple modules."

[dependencies]
snafu = { workspace = true }
miji_core = { path = "../core" }
