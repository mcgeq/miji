[package]
name = "todos"
version = "0.1.0"
edition = "2024"
description = "To-Do list feature with task creation, editing, and filtering."

[dependencies]
snafu = { workspace = true }
common = { path = "../common" }
