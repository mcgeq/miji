[package]
name = "settings"
version = "0.1.0"
edition = "2024"
description = "Application settings such as theme, notifications, privacy, and configuration."

[dependencies]
snafu = { workspace = true }
common = { path = "../common" }
