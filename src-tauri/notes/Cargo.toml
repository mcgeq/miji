[package]
name = "notes"
version = "0.1.0"
edition = "2024"
description = "Note-taking module for creating, editing, and organizing personal notes."

[dependencies]
snafu = { workspace = true }
miji_core = { path = "../core" }
